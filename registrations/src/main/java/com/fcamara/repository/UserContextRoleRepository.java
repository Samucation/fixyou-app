package com.fcamara.repository;

import com.fcamara.model.UserContextRole;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserContextRoleRepository extends JpaRepository<UserContextRole, Long> {
}
